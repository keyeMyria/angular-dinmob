import { MyCollapseDirective } from './my-collapse.directive';

describe('MyCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new MyCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
