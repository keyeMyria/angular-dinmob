import { PrototiposToStringPipe } from './prototipos-to-string.pipe';

describe('PrototiposToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PrototiposToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
